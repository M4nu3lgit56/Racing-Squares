// inicializando variables
const container = document.getElementById('arena');
const square = document.getElementById('sqr');
const rect = container.getBoundingClientRect();
const sqrRect = square.getBoundingClientRect();

// 
